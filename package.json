{
  "name": "root",
  "private": true,
  "devDependencies": {
    "coveralls": "^3.0.2",
    "lerna": "^3.3.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "lerna run lint",
    "test": "lerna run test",
    "test:with-coverage": "lerna run test:with-coverage",
    "build": "lerna run build"
  }
}
