{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.3.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "lerna run lint",
    "test": "lerna run test",
    "build": "lerna run build"
  }
}
