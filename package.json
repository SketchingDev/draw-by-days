{
  "name": "root",
  "private": true,
  "devDependencies": {
    "coveralls": "^3.0.2",
    "lerna": "^3.3.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "lerna run lint --stream",
    "test": "lerna run test --stream",
    "test:with-coverage": "lerna run test:with-coverage --stream",
    "test:component": "lerna run test:component --stream",
    "build": "lerna run build --stream"
  }
}
