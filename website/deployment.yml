apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: website
spec:
  template:
    metadata:
      labels:
        app: website
        tier: frontend
    spec:
      containers:
      - name: drawbydays-website
        image: sketchingdev/drawbydays-website:0.0.1-SNAPSHOT
        ports:
        - containerPort: 80
        env:
        - name: SERVER_PORT
          value: "80"
---
apiVersion: v1
kind: Service
metadata:
  name: website
  annotations:
    external-dns.alpha.kubernetes.io/hostname: drawbydays.com
spec:
  type: LoadBalancer
  ports:
  - port: 80
  selector:
    tier: frontend
