{
  "name": "website",
  "version": "1.0.0",
  "private": true,
  "description": "Draw By Days website",
  "main": "handler.js",
  "scripts": {
    "deploy:dev": "REACT_APP_DAILY_IMAGE_API_URL=https://api-dev.drawbydays.com yarn build && yarn deploy:domain --stage dev && yarn deploy:s3 --stage dev",
    "deploy:s3": "sls client deploy --no-confirm",
    "deploy:domain": "sls deploy -v",
    "remove:dev": "yarn remove:domain --stage dev && yarn remove:s3 --stage dev",
    "remove:s3": "sls client remove --no-confirm",
    "remove:domain": "sls remove -v",
    "clean": "rimraf build/*",
    "start": "react-scripts-ts start",
    "build": "yarn clean && yarn react-scripts-ts build",
    "test": "CI=true react-scripts-ts test --env=jsdom",
    "eject": "react-scripts-ts eject",
    "start:mock": "json-server mock/dailyImageApi.json --port=3001 --static ./mock/public"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-router-dom": "^4.3.1",
    "react-scripts-ts": "3.1.0",
    "semantic-ui-css": "^2.4.1",
    "semantic-ui-react": "^0.85.0",
    "serverless-finch": "^2.4.2",
    "serverless-stack-output": "^0.2.3",
    "serverless-stage-manager": "^1.0.5"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/jest": "^24.0.6",
    "@types/node": "^11.9.4",
    "@types/react": "^16.8.4",
    "@types/react-dom": "^16.8.2",
    "@types/react-router-dom": "^4.3.1",
    "daily-image-api": "^1.0.0",
    "json-server": "^0.15.0",
    "typescript": "^3.3.3333"
  }
}
