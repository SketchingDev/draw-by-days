language: node_js

node_js: 8

install:
  - yarn install
  - yarn bootstrap

script:
  - yarn lint
  - yarn test
  # --stream causes more output so CI doesn't timeout
  - yarn build --stream

cache: yarn
